# Auto detect text files and perform LF normalization
* text=auto

# 1. Ignore everything by default
* export-ignore

# 2. Whitelist the addons folder (folder itself AND contents)
!addons/
!addons/**
